{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        "products": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "customers": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        },
        "sales": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        }
      }
    }
  }
}